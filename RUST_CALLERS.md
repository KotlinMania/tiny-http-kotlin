# Rust callers — `tiny-http-kotlin`

> Generated by `scaffold/generate_caller_reports.py` from `workspace_dep_graph.json` (Cargo-aware dep graph). Do not hand-edit. Re-run after the graph is rebuilt.

## What this file is

For every Rust crate this repo owns (under `tmp/`), this file lists every **cross-repo** workspace caller: every `use` statement in another `*-kotlin` repo's Rust source that imports a symbol from one of this repo's crates. Symbols are grouped per-crate; codex-kotlin sites are surfaced first per symbol (codex is the priority consumer driving the port queue).

**Granularity:** import-level (each row is one `use` line). Call-site detail — where the imported name is actually invoked, implemented, pattern-matched, or used as a generic bound inside the caller body — requires a per-symbol grep across the listed caller files. The graph tells you exactly which files to grep.

**Cross-repo only.** Imports between crates inside this same repo are omitted — those are intra-repo refactoring concerns, not the cross-project porting concern this file exists to surface.

**Cargo-resolved.** Counts here honor `package = "..."` renames, `workspace = true` inheritance, and `[target.<cfg>.dependencies]`. Stdlib (`std`, `core`, `alloc`) and intra-crate sub-module references are excluded.

## Crates in this repo

| package | module | inbound use sites | distinct caller crates | distinct caller repos | codex sites |
|---|---|---:|---:|---:|---:|
| `tiny_http` | `tiny_http` | 17 | 3 | 1 | 17 |

## `tiny_http`

### Inbound symbols (6 distinct, 17 total `use` sites)

Sorted by codex demand, then total demand. The first symbols are the priority queue for porting `tiny_http`.

#### `tiny_http::Header` — 4 site(s) (4 codex, 0 other, 2 caller crates)

- ✅ `codex-kotlin/codex-login` `src/server.rs:42` — `use tiny_http::Header`
- ✅ `codex-kotlin/codex-responses-api-proxy` `src/dump.rs:13` — `use tiny_http::Header`
- ✅ `codex-kotlin/codex-responses-api-proxy` `src/dump.rs:217` — `use tiny_http::Header`
- ✅ `codex-kotlin/codex-responses-api-proxy` `src/lib.rs:24` — `use tiny_http::Header`

#### `tiny_http::Method` — 3 site(s) (3 codex, 0 other, 1 caller crates)

- ✅ `codex-kotlin/codex-responses-api-proxy` `src/dump.rs:14` — `use tiny_http::Method`
- ✅ `codex-kotlin/codex-responses-api-proxy` `src/dump.rs:218` — `use tiny_http::Method`
- ✅ `codex-kotlin/codex-responses-api-proxy` `src/lib.rs:25` — `use tiny_http::Method`

#### `tiny_http::Response` — 3 site(s) (3 codex, 0 other, 3 caller crates)

- ✅ `codex-kotlin/codex-login` `src/server.rs:44` — `use tiny_http::Response`
- ✅ `codex-kotlin/codex-responses-api-proxy` `src/lib.rs:27` — `use tiny_http::Response`
- ✅ `codex-kotlin/codex-rmcp-client` `src/perform_oauth_login.rs:13` — `use tiny_http::Response`

#### `tiny_http::Server` — 3 site(s) (3 codex, 0 other, 3 caller crates)

- ✅ `codex-kotlin/codex-login` `src/server.rs:45` — `use tiny_http::Server`
- ✅ `codex-kotlin/codex-responses-api-proxy` `src/lib.rs:28` — `use tiny_http::Server`
- ✅ `codex-kotlin/codex-rmcp-client` `src/perform_oauth_login.rs:14` — `use tiny_http::Server`

#### `tiny_http::Request` — 2 site(s) (2 codex, 0 other, 2 caller crates)

- ✅ `codex-kotlin/codex-login` `src/server.rs:43` — `use tiny_http::Request`
- ✅ `codex-kotlin/codex-responses-api-proxy` `src/lib.rs:26` — `use tiny_http::Request`

#### `tiny_http::StatusCode` — 2 site(s) (2 codex, 0 other, 2 caller crates)

- ✅ `codex-kotlin/codex-login` `src/server.rs:46` — `use tiny_http::StatusCode`
- ✅ `codex-kotlin/codex-responses-api-proxy` `src/lib.rs:29` — `use tiny_http::StatusCode`
